{
  "name": "discreate-telemetry-server",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.0.0"
  }
}
